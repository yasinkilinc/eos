package com.example.orders;

/**
 * Everything in this Javadoc is prose, including a closing brace } and the
 * words class Ghost and implements Phantom and the annotation @Service.
 */
public class Trap {
    // class Commented implements Nothing {
    private static final String RULE =
        "@Service classes must not depend on adapters, and class Fake implements Bad";

    public String rule() { return RULE; }
}
