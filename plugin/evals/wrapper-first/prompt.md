---
description: A task that needs a wrapper the project declares; with the plugin the task brief names it before the first call. Read-only tools only (Bash-granting evals cannot run on every machine).
runs: 1
max_turns: 6
allowed_tools: [Read, Glob, Grep]
tags: [wrappers]
---

Which exact command should I run in this project to get the status of ticket DEMO-1 from the tracker? Answer with the command line only.
